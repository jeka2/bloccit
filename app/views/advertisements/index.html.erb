<h1>Advertisement</h1>
<% @ad.each do |advert| %>
<div class="media">
  <div class="media-body">
    <h4 class="media-heading">
      <%= link_to advert.title, advert %>
    </h4>
  </div>
</div>

<% end %>
